import { defineConfig } from 'windicss/helpers'

export default defineConfig({
  darkMode: false,
  theme: {
    colors: {
      'black': '#000000',
      'grey-light': '#F7F7F7',
      'white': '#FFFFFF'
    },
    fontFamily: {
      'monospace': ['Arimo', 'sans-serif']
    }
  }
})